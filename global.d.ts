interface Window {
    YT: {
      PlayerState: {
        PLAYING: number;
        PAUSED: number;
        // Add other PlayerState properties if needed
      };
      // Add other YT properties if needed
    };
  }
  